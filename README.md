fork  https://github.com/vuesax-alpha/vuesax-alpha
a vue component

```shell
$env:TAG_VERSION="1.0.5"; $env:GIT_HEAD=(git rev-parse HEAD); npm run update:version
npm run build
cd packages/vuesax-alpha && npm publish
```
